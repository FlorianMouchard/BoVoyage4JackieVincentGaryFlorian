@model IEnumerable<BoVoyage4.Models.AgenceVoyage>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/BackOffice/Views/Shared/_LayoutBO.cshtml";
}

<h2>Liste des agences de voyage</h2>

<p>
    <a href="@Url.Action("Create")" class="btn btn-outline-info btn-sm">Créer une nouvelle agence de voyage</a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nom)
        </td>
        <td>
            <a href="@Url.Action("Edit", new { id=item.ID})"><i class="fa fa-edit"> | </i></a>
            <a href="@Url.Action("Details", new { id=item.ID})"><i class="fa fa-eye"> | </i></a>
            <a href="@Url.Action("Delete", new { id =item.ID })"><i class="fa fa-trash"></i></a>
        </td>
    </tr>
}

</table>
